{
  "name": "bigdaddy",
  "version": "1.0.0",
  "description": "In this activity, you will initialize your first Node project using `npm` and install (and save) the `inquirer` package.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "philip",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^7.0.0"
  }
}
